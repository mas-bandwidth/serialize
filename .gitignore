# build directories
build*/
out/
cmake-build-*/

# cmake generated files (from in-source configuration: prefer building out of source)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
CPackConfig.cmake
CPackSourceConfig.cmake
serialize-config-version.cmake
compile_commands.json
_deps/
Testing/
/Makefile

# built binaries and intermediates
/bin/
/test
/example
/fuzz
*.o
*.obj
*.a
*.lib
*.exe
*.dll
*.dylib
*.so
*.pdb
*.ilk
*.dSYM/

# visual studio / xcode project generation
.vs/
*.sln
*.vcxproj
*.vcxproj.*
*.xcodeproj/

# the conformance checker, when it is built rather than run with `go run .`
/tools/conformance/conformance

# libfuzzer artifacts
crash-*
oom-*
timeout-*
slow-unit-*
corpus/

# editors and os
.DS_Store

# Bench-local scratch: working notes, logs and generated corpora.
# Never repo content — this ignore is what keeps it that way.
tending/
